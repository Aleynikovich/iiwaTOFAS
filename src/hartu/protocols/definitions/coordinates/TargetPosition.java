package hartu.protocols.definitions.coordinates;

public abstract class TargetPosition {
    // This abstract class can define common methods or properties for all target positions
    // For now, it's a marker for polymorphism.
    // Subclasses will implement specific ways to represent position data.

    @Override
    public abstract String toString();
}