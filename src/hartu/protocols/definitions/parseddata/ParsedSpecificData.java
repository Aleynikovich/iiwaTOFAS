package hartu.protocols.definitions.parseddata;

public abstract class ParsedSpecificData {
    // This abstract class acts as a marker interface or base for specific parsed data containers.
    // Subclasses will hold the actual parsed fields (e.g., target position, IO state).
}