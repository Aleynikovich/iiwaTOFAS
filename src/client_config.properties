# Client Network Configuration (robot acts as client)
# Note: Joint state is now published via server (see server_config.properties)
# This configuration is kept for backward compatibility but jointLog.port is no longer used
# Ports for the services the client connects to (DEPRECATED)
# jointLog.port=30002